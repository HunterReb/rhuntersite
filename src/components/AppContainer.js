import styled from "styled-components";

const AppContainer = styled.AppContainer`
    background: white;
    width 100%;
    height: auto;
    z-index:1;
    `

export default AppContainer

// Z index can be used to layer components, lower is lower, higher is higher